{
  "name": "@context-grabber/extension-safari",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "bun build src/runtime/background-main.ts --outdir dist/runtime --target=browser --conditions=bun --format=iife && bun build src/runtime/content-main.ts --outdir dist/runtime --target=browser --conditions=bun --format=iife",
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit",
    "test": "bun test",
    "native-messaging": "bun src/native-messaging-cli.ts"
  },
  "dependencies": {
    "@context-grabber/extension-shared": "workspace:*",
    "@context-grabber/shared-types": "workspace:*"
  }
}
