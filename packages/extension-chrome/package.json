{
  "name": "@context-grabber/extension-chrome",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target=browser --conditions=bun --format=iife && bun build src/extract-active-tab.ts --outdir dist --target=browser --conditions=bun --format=iife",
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit",
    "test": "bun test",
    "native-messaging": "bun run src/native-messaging-cli.ts"
  },
  "dependencies": {
    "@context-grabber/extension-shared": "workspace:*",
    "@context-grabber/shared-types": "workspace:*"
  }
}
