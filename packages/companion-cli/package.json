{
  "name": "@context-grabber/companion-cli",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit",
    "test": "bun test",
    "start": "bun src/index.ts"
  },
  "dependencies": {
    "@context-grabber/native-host-bridge": "workspace:*",
    "@context-grabber/shared-types": "workspace:*"
  }
}
